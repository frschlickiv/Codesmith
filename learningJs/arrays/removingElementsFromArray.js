const numbers = [1, 2, 3, 4]


// FOR ADDING ELEMENTS

// End
numbers.push()

// Beginning
    numbers.unshift()

// Middle 

numbers.splice()


// For DELETING ELEMENTS

// End
const last = numbers.pop();
console.log(numbers);
console.log(last);

// Beginning
/* */  const first = numbers.shift()

   console.log (first);
   console.log(numbers);

// Middle 

numbers.splice(2, 2)

//first argument tells you what index to start at then the second number is how many indicies to remove. 